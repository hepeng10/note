1. 什么是闭包？
2. cookie，localStorage，sessionStorage 的区别
3. 对原型链的理解
4. 对面向对象编程和函数式编程的理解
5. == 和 === 的区别
6. 基本数据类型和引用数据类型的区别
7. 箭头函数和普通函数的区别
8. 如何判断一个变量的类型
9. 重绘和重排是什么
11. 盒模型是什么
12. TCP 和 UDP 的区别
13. HTTP 常用状态码
14. 如何解决跨域
15. 事件循环（event loop）是什么
16. react 组件间通信方式
17. 什么是受控组件，什么是非受控组件
18. 什么是虚拟 DOM，fiber 呢？
19. 数组去重
20. 数组删除任意元素
21. 冒泡排序

```ts
function Person() {
    const [name, setName] = useState('张三');15
    console.log(name);2

    const onClick = () => {
        setName('李四');4
    }

    return <div onClick={onClick}>{name}</div>3
}
```

```js
function Person() {
    this.name = '张三'
}

Person.prototype.getName = () => {
    console.log(this.name)
}

const p1 = new Person();
const p2 = new Person();
const p1 = new Person();
const p1 = new Person();
p1.__proto__

class Person {
    constructor() {
        this.name = '111'
        this.getName = () =>{}
    }
    getName() {}
}
```
```js
const obj = {
    name: 'xxx'
}
function fn(o) {
    o.name = 'b'
}
fn(obj)
console.log(obj.name)

let a = 'x'
function f(v) {
    v = 'y'
}
f(a)
console.log(a)
```