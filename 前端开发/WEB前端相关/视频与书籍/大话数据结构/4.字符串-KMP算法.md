## 字符串的普通匹配算法
查询一个字符串 C 是否为另一个字符串 P 的子串，首先能想到的最普通的算法是：
1. 从 P 串第一位开始，与 C 串进行逐一对比，若相等就对比下一个字符，若不等则终止。
2. 从 P 串第二位开始，与 C 串进行追一对比。。。
3. 继续对比，直到与 C 串所有字符都相等，则匹配成功。若 P 串开始对比时，剩余的字符长度还比 C 串小，则说明匹配不成功，结束。

## KPM 算法——模式匹配
有点复杂，还需要多次琢磨
