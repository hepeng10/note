# vscode
保存时会自动删除 return 后的代码，是 vscode 配置默认行为，需要修改配置文件：
```json
    "editor.codeActionsOnSave": {
        "source.fixAll": "never", // 禁用所有的，默认值是 explicit
        "source.fixAll.eslint": "explicit", // 启用 eslint
        "source.fixAll.stylelint": "explicit" // 启用 stylelint
    },
```